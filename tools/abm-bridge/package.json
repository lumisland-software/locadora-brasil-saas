{
  "name": "locadora-brasil-abm-bridge",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run sync",
    "login": "node login.mjs",
    "sync": "node sync.mjs && node upload.mjs",
    "upload": "node upload.mjs",
    "discover": "node discover.mjs",
    "check": "node --check index.mjs && node --check login.mjs && node --check sync.mjs && node --check upload.mjs && node --check discover.mjs"
  },
  "dependencies": {
    "playwright": "^1.54.1"
  }
}
